# Legacy debug helpers

One-off operational scripts kept for recovery, probing, and manual cloud-flow
debugging. These are not primary entrypoints for the modernized workflow.

Prefer `python -m scamper_legacy.cli ...` for planned legacy provider runs.
